# マカロンの音声ファイル置き場

このフォルダに `.mp3` (または `.wav` / `.ogg` / `.m4a`) ファイルを置くと、
Discord上で `/play` コマンドから再生できます。

例:
- `sounds/makaron/hello.mp3` を置く → `/play name:hello` で再生

ファイル名(拡張子なし)がそのままコマンドの候補として
オートコンプリートに表示されます。
